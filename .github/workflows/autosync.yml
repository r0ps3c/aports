name: Sync Fork

on:
  schedule:
    - cron: '9 1 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@00a7d337fe4e748bf3c2051c3450a9acd14c401e
        with:
          base: 3.17-stable
          head: 3.17-stable
