name: Sync Fork

on:
  schedule:
    - cron: '9 1 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@26ff3d32adc6b641d08801c1301cb6101b3ff0e7
        with:
          base: 3.17-stable
          head: 3.17-stable
