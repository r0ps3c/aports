name: Sync 3.18-stable

on:
  schedule:
    - cron: '20 4,11,16 * * *'
  workflow_run:
    workflows: [Sync Fork]
    types:
      - completed
    branches: 
      - master
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@v3
        with:
          base: 3.18-stable
          head: 3.18-stable
