name: Sync Fork

on:
  schedule:
    - cron: '55 15 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@v3
        with:
          base: 3.18-stable
          head: 3.18-stable
