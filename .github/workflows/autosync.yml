name: Sync Fork

on:
  schedule:
    - cron: '9 1 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@e742e0bbb3a8d495c68f5615be8db0acb3b36a7b
        with:
          base: 3.17-stable
          head: 3.17-stable
