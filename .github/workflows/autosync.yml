name: Sync Fork

on:
  schedule:
    - cron: '2 1,12 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@v3
        with:
          base: master
          head: master
  call-workflow:
    uses: r0ps3c/aports/.github/workflows/autosync.yml@3.18-stable
