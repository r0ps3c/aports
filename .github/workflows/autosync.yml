name: Sync Fork

on:
  schedule:
    - cron: '9 1 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@aa19394873b8c10b4309cb833f10c12cf53b5efe
        with:
          base: 3.17-stable
          head: 3.17-stable
