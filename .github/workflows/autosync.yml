name: Sync Fork

on:
  schedule:
    - cron: '9 1 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@ca96eb1d737f04aac12e0f16d3201b9aa8d0bc6b
        with:
          base: 3.17-stable
          head: 3.17-stable
