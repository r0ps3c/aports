name: Sync Fork

on:
  schedule:
    - cron: '9 1 * * *' # sync once a day
  workflow_dispatch: # on button click

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - uses: r0ps3c/fork-sync@297cecbfac82c785df5e59221adc0526918585e5
        with:
          base: 3.17-stable
          head: 3.17-stable
